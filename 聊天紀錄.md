# RSP_demo 專案 - 聊天與開發紀錄

## 2026-05-12

### 1. 專案理解與分析
- **討論內容**：AI 詳細分析了 `RSP_demo` 專案的結構（包含 `README.md`, `train_svm.py`, `test.py` 等），確認這是一個預計要在樹莓派 4 (Raspberry Pi 4) 上執行的「剪刀、石頭、布」手勢辨識專題。
- **作業目標**：
  - 成功實機執行 (50%)。
  - 攝影機 Demo 影片，展示 15 個手勢 (15%)。
  - 報告：需尋找並替換**兩種新的模型架構**進行訓練，比較差異，並列出 Accuracy, Precision, Recall, F1-score (35%)。

### 2. 完善攝影機 Demo 腳本 (`carema.py`)
- **實作內容**：
  - 導入 `argparse`，新增 `-m` 或 `--model` 參數，允許在指令後方動態選擇要讀取的模型檔案，方便日後 Demo 不同的模型。
  - 加入與訓練檔一致的影像前處理邏輯（轉灰階 $\rightarrow$ 縮放 64x64 $\rightarrow$ 攤平並正規化）。
  - 加入模型載入與即時推論，並將預測結果顯示在畫面上。
  - 考量到樹莓派的效能，特別加入了即時 FPS 計算與顯示。
- **二次修正**：為了避免攝影機畫面比例造成手部變形，在畫面上新增了 300x300 的 ROI (感興趣區域) 黃色提示框，引導使用者將手放在框內進行截圖辨識。

### 3. 模型預測卡死問題分析 (Out-of-Distribution 問題)
- **問題描述**：使用者回報在樹莓派端執行 `carema.py` 時，即便 `test.py` 有 68% 準確率，攝影機的預測結果仍完全卡死，不會隨手勢變更。
- **原因分析 (AI 理論推導)**：
  1. **RBF Kernel 數學特性**：SVM 的 RBF Kernel 藉由計算空間距離來判斷相似度。當真實攝影機的背景或光線與訓練集差異極大時，兩者在 4096 維空間中的距離會非常遠，導致 Kernel 計算的相似度趨近於 0，模型最終只會無腦輸出一個固定的 Bias (偏差) 類別。
  2. **攤平像素 (Flatten) 失去空間特徵**：直接把圖片拉平使得背景像素佔據極大影響力，缺乏對背景雜訊的抵抗能力。
  3. **資料分佈不一致**：`test.py` 的測試集與訓練集來自相同拍攝環境（同資料分佈），因此準確率正常；但使用者的 Webcam 環境是全新的分佈 (Out-of-Distribution)，因此導致模型崩潰。
- **解決方向**：這正好能作為報告中「為何要替換模型」的最佳理由。AI 建議後續實作具有特徵擷取能力的 **CNN (如 MobileNetV2)** 或另一種傳統模型 **Random Forest** 進行對比。

### 4. 建立對話與開發紀錄機制
- **動作**：使用者要求將過去的對話整理成 `聊天紀錄.md`，並建立自動維護此檔案的機制 (Skill)。

### 5. 升級智慧型相機腳本 (`carema.py`)
- **使用者需求**：詢問若使用不同架構的模型（如 CNN 或 Random Forest），是否需要寫不同的相機程式。
- **實作內容**：AI 將 `carema.py` 升級為「智慧型相機腳本」，透過判斷副檔名動態決定載入與前處理邏輯：
  - `.pkl`：傳統機器學習模型 (SVM, Random Forest)，使用 `joblib` 載入，圖片轉灰階並攤平為一維陣列。
  - `.h5` / `.keras`：TensorFlow/Keras CNN 模型，保留 RGB 通道並增加 Batch 維度 `(1, 64, 64, 3)`。
  - `.pt` / `.pth`：PyTorch CNN 模型，轉為 `(1, 3, 64, 64)` 的 Tensor 格式。
  - **模組延遲載入 (Lazy Import)**：只在需要時才載入對應套件 (如 `import torch` 或 `tensorflow`)，避免樹莓派因為載入不必要的龐大套件而導致程式開啟過慢或記憶體崩潰。
- **效益**：日後不論訓練出何種模型，只需使用同一個 `carema.py` 即可順暢 Demo。

### 6. 重新理解專案結構與紀錄維護規則
- **本次確認的專案結構**：
  - `README.md`：說明樹莓派刷機、執行流程與作業評分標準。
  - `train/train_svm.py`：從 `dataset/train` 與 `dataset/test` 讀取影像，做灰階、縮放、攤平與正規化後訓練 SVM，並把模型輸出到 `demo/rps_svm_model.pkl`。
  - `demo/test.py`：載入已訓練的 `.pkl` 模型，對測試集重新評估並輸出 accuracy 與 classification report。
  - `demo/carema.py`：即時攝影機推論程式，支援傳統 ML 與 CNN 類型模型。
- **本次確認的紀錄維護機制**：
  - 專案內並沒有獨立的 slash skill；真正的規則寫在 `.cursorrules`。
  - `.cursorrules` 明確要求：只要對話中有「實質進展、分析或程式修改」，就必須更新根目錄的 `聊天紀錄.md`。
  - 紀錄內容需包含：討論摘要、程式變更、問題分析、決策結果，並以繁體中文、清楚標題與條列方式追加。
- **後續執行方式**：AI 後續在此專案中的每次有效分析、修改或階段性成果，都應同步追加到 `聊天紀錄.md`，不需等待使用者再次提醒。
